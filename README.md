# Tcp-Chatroom-Using-Python
The TCP Chatroom is a real-time messaging app built using Python’s socket and threading modules. It uses a client-server architecture where the server manages multiple clients and broadcasts messages via TCP for reliable communication. The project demonstrates socket programming, multithreading, and synchronized data transmission in networks.
